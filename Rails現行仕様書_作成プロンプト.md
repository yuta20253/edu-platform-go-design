# Rails現行仕様書 自動生成プロンプト

## 目的

既存Railsアプリケーションのソースコード全体を解析し、
現在のシステム仕様を整理した「Rails現行仕様書」を作成してください。

この資料の目的は、

「現在のRailsシステムがどのような機能を提供し、
どのような処理フロー・データ構造・業務ルールで動作しているか」

を明確にすることです。

この資料は、将来的にGoなど別技術へ移行する際に、
現行システムの仕様を理解するために利用します。

ただし、Railsの実装方法やディレクトリ構造をそのまま新システム設計へ引き継ぐことは目的ではありません。

---

# 基本方針

以下を厳守してください。

- Railsコードの説明書を作成しない
- ControllerやService単位ではなく、業務機能単位で整理する
- Rails固有の実装ではなく、システムの振る舞いを記載する
- 利用者操作、業務ルール、データの流れを中心に整理する
- 実装詳細は必要最低限の参考情報として扱う
- 不明な仕様は推測せず「確認できない事項」として記載する

---

# 調査対象

Railsプロジェクト全体を解析し、
対象機能に関係する以下の実装を確認してください。

## Controller

確認内容:

- 提供している機能
- Action一覧
- HTTP Method
- Endpoint
- Request Parameter
- 認証処理
- 権限制御
- Response内容

---

## Service

確認内容:

- 実行される業務処理
- 処理フロー
- 条件分岐
- データ作成・更新・削除
- 副作用

※Serviceクラスの構造説明ではなく、
「システム上何を実現している処理なのか」を整理してください。

---

## Form Object

確認内容:

- 入力項目
- 必須条件
- バリデーション
- 業務的な制約

---

## Model

確認内容:

- 利用テーブル
- データ構造
- Association
- Validation
- Enum
- 状態管理
- 業務ルール

---

## Query

存在する場合:

- 検索条件
- 絞り込み条件
- 並び順
- データ取得ルール

---

## Serializer

確認内容:

- APIレスポンス項目
- 表示用データ加工
- 返却データ構造

---

## Job / Mailer

存在する場合:

- 非同期処理
- バッチ処理
- メール送信処理
- 自動実行される処理

---

## Migration / Schema

確認内容:

- テーブル構造
- カラム
- 制約
- テーブル間の関係

---

## Test

RSpecなどのテストコードが存在する場合は、
仕様確認の参考情報として利用してください。

確認内容:

- 正常系
- 異常系
- 権限制御
- 境界条件
- 想定されている利用方法

---

# 機能分類ルール

Railsのディレクトリ構造ではなく、
利用者視点・業務目的で機能を分類してください。

分類基準:

- ユーザーが何をする機能なのか
- どの業務目的を達成するものなのか
- 関連するModel・Service・Controllerが何か

---

## 悪い分類例

```
StudentController機能

TaskService機能

AdminService機能
```

理由:

Railsの技術構造であり、利用者視点の機能ではないため。

---

## 良い分類例

以下の実装:

```
Student::TasksController

Student::CreateTaskService

Student::UpdateTaskService

Taskモデル

TaskSerializer
```

↓

```
タスク管理機能
```

---

# 出力ファイル

以下の形式でMarkdownファイルを作成してください。

## 1. 全体機能一覧

最初に以下を作成してください。

```
Rails現行機能一覧.md
```

内容:

# Rails現行機能一覧

|機能名|概要|主な利用者|
|-|-|-|
|認証機能|ログイン・認証処理|全ユーザー|
|タスク管理機能|学習タスク管理|生徒|

---

## 2. 各機能仕様書

機能ごとに以下の形式で作成してください。

ファイル名:

```
〇〇機能_Rails現行仕様書.md
```

例:

```
認証機能_Rails現行仕様書.md

タスク管理機能_Rails現行仕様書.md

学習分析機能_Rails現行仕様書.md

お知らせ機能_Rails現行仕様書.md
```

---

# 機能仕様書フォーマット

# 〇〇機能 Rails現行仕様書

---

# 1. 機能概要

記載内容:

- 機能の目的
- システム上の役割
- 利用者

---

# 2. 機能一覧

この機能で提供される操作を整理してください。

例:

|操作|概要|
|-|-|
|一覧取得|登録データを取得|
|新規登録|新しいデータを作成|
|更新|既存データを変更|

---

# 3. 業務フロー

利用者操作からシステム処理完了まで整理してください。

例:

1. ユーザーが情報入力
2. 入力値を検証
3. データを保存
4. 結果を返却

---

# 4. API一覧

関連するController Actionを一覧化してください。

|Controller|Action|HTTP Method|Endpoint|概要|
|-|-|-|-|-|

---

# 5. API / 処理詳細

機能仕様書は機能単位で作成します。

ただし、詳細部分はController Action単位で整理してください。

---

## Controller名#Action名

例:

## TasksController#create

### 概要

この処理が提供する機能を説明してください。

---

### Request

入力値を整理してください。

|項目|必須|型|説明|
|-|-|-|-|

---

### 処理内容

処理の流れを整理してください。

例:

1. 入力値を検証
2. 権限確認
3. データ作成
4. レスポンス返却

---

### 業務ルール

システム上の条件や制約を記載してください。

例:

- 本人のみ登録可能
- 期限日は過去日不可

---

### Database変更

作成・更新・削除されるデータを整理してください。

例:

作成:

```
tasks
```

更新:

```
users
```

---

### Response

返却データを整理してください。

---

### Errorケース

|条件|HTTP Status|内容|
|-|-|-|

---

# 6. データモデル

関連するテーブルを整理してください。

記載内容:

- テーブル名
- 役割
- 主なカラム
- リレーション

---

# 7. 状態管理

状態変化がある場合整理してください。

例:

```
not_started

↓

completed
```

状態変更条件:

- 完了操作実行時
- 条件達成時

---

# 8. 権限制御

以下を整理してください。

- 利用可能ユーザー
- 閲覧制限
- 操作制限

---

# 9. 非同期処理

JobやMailerが存在する場合整理してください。

記載内容:

- 実行タイミング
- 処理内容
- 影響範囲

---

# 10. Rails実装対応表

最後に参考情報として記載してください。

例:

|種類|実装|
|-|-|
|Controller|Student::TasksController|
|Service|Student::CreateTaskService|
|Form|Student::CreateTaskForm|
|Model|Task|
|Serializer|TaskSerializer|

注意:

この情報は現在実装との対応確認用です。

新システム設計へRails構造をそのまま引き継ぐ目的ではありません。

---

# 禁止事項

以下は禁止してください。

- Railsコードの逐語的な説明
- Controller一覧だけの資料作成
- Serviceクラス説明書の作成
- ActiveRecord記法を仕様として記載する
- Callback実装をそのまま業務ルールとして扱う
- Rails固有パターンを設計ルールとして扱う
- Goへの変換案を書く
- コードから確認できない仕様を推測する

---

# 判断基準

## 悪い例

```
CreateTaskServiceでTask.create!を実行する。
```

## 良い例

```
入力されたタスク情報を検証し、
生徒に紐づく学習タスクを登録する。
```

---

## 悪い例

```
before_save callbackでcompleted_atを更新する。
```

## 良い例

```
タスクが完了状態になると、
完了日時が保存される。
```

---

# 完成条件

生成された仕様書だけを読むことで、
Railsコードを知らない開発者でも以下を理解できる状態にしてください。

- システム全体の機能
- 各機能の目的
- 利用者
- 操作フロー
- API仕様
- データ構造
- 業務ルール
- 権限制御
- 状態変化

作成物は、

「Rails実装資料」

ではなく、

「現在稼働しているシステムの現行仕様書」

として作成してください。
