# お知らせ機能 Go移行・設計仕様書

---

# 1. 機能概要

## 機能概要

生徒に対して、対象範囲判定済みの公開お知らせを一覧・詳細で提供する参照機能である。Rails現行仕様では、`Announcement.for_user`により生徒の所属高校・役割・学年・ユーザー単位の対象条件を判定し、`published`状態のお知らせのみを取得する。

## 利用者

- 生徒ユーザー（student ロール）
- 自分に配信対象となるお知らせのみ参照可能

## 業務上の目的

- 学校・運営からの通知情報を、対象となる生徒に限定して届ける
- 公開済みのお知らせのみを表示し、下書き・予約段階の情報を生徒に見せない
- お知らせ一覧・詳細を安定したページングと並び順で提供する

---

# 2. 設計方針

本機能は参照系のみで完結するため、複雑なドメイン構造を持ち込まず、対象範囲判定と状態フィルタを明確に分離したシンプルな設計とする。

- 責務分離: 対象範囲判定・状態フィルタ・ページング・レスポンス整形を分離する
- 保守性: 対象判定条件（学年・役割・所属高校・個人）の追加・変更に強い構造とする
- テスト容易性: 一覧・詳細取得の挙動をUseCase単位で検証できるようにする
- API互換性: 既存エンドポイント・レスポンス構造を維持する
- 拡張性: 将来的な既読管理・お知らせ種別追加に備え、参照ロジックと表示ロジックを分離しておく

---

# 3. Bounded Context

## Context名

- announcement（お知らせ配信）

## Contextの責務

- 公開済みお知らせの対象範囲判定
- 生徒向けお知らせの一覧・詳細参照

## 他Contextとの依存関係

- User Context: 生徒の所属高校・役割・学年など、対象判定に必要な属性情報に依存する

## 依存する理由

お知らせの対象範囲判定は、生徒自身の属性情報（所属高校・役割・学年）に基づいて行われるため、User Contextが保持する情報を参照する必要がある。なお、お知らせの作成・スケジュール公開（`draft`→`scheduled`→`published`の状態遷移）は本機能（生徒向け参照）の対象外であり、別の管理者向け機能・非同期ジョブの責務として扱う。

---

# 4. 設計パターン

## 採用パターン

Transaction Script

## 判断根拠

本機能は一覧取得・詳細取得の2操作のみで構成される参照系機能であり、生徒側で状態を変更する操作は存在しない。お知らせの状態遷移（`draft`→`scheduled`→`published`）はRails現行仕様においても非同期ジョブ（`PublishScheduledAnnouncementsJob`）側の責務であり、本機能にとっては「`published`のみを対象とする」という単純なフィルタ条件に過ぎない。

対象範囲判定（所属高校・役割・学年・ユーザー単位）は複数条件の組み合わせではあるが、入力（生徒属性）に対して検索条件を組み立てるだけの処理であり、状態を持つEntityに振る舞いを持たせる必然性がない。以下の理由からTransaction Scriptを採用する。

- 業務ルール: 対象判定は「条件に合致するかどうか」の判定であり、Entity間の複雑な相互作用がない
- 状態管理: 本機能側では状態を変更しない（読み取り専用）
- 将来拡張: 既読管理などの機能が追加された場合でも、参照処理自体はTransaction Scriptのままで対応でき、状態を持つ機能が必要になった時点でその部分のみDomain Model化を検討すればよい
- テスト容易性: UseCase単位で入力条件と出力結果を検証すればよく、Entityの内部状態遷移を検証する必要がない

## 採用しなかったパターン

### Active Record

生徒向け参照では作成・更新処理が発生しないため、モデルに保存・更新の責務を持たせる意味がない。

### Domain Model

状態遷移（公開処理）は本機能のスコープ外であり、本機能側のEntityに状態変化を持たせる必要がない。対象範囲判定は条件が複数あるものの、あくまで検索条件の組み立てであり、Entityの振る舞いとして表現するメリットが薄い。

### Event Sourcing

お知らせの参照において、変更履歴の再構築や監査要件は現行仕様に存在しない。

---

# 5. Aggregate設計

不要と判断する。

理由: 本機能は参照のみであり、書き込みの整合性を保証すべき境界が存在しない。Announcementは単一Entityとして扱い、Aggregateの概念を導入する必要はない。

---

# 6. Entity設計

## Announcement

- 役割: 生徒へ配信される通知情報を表す中心的な概念
- ライフサイクル: 作成・スケジュール・公開は本機能のスコープ外（他機能で完結）。本機能では既に`published`となったお知らせを参照するのみ
- 状態変化: 本機能内では発生しない（読み取り専用）
- 保持する責務: タイトル・本文・公開日時・発行者情報などの属性を保持する
- 判断根拠: 生徒が参照する情報の中心的な概念であるため

## Publisher（User参照）

- 役割: お知らせの発行者情報を表示するための参照情報
- ライフサイクル・状態変化: 本機能では扱わない
- 保持する責務: id・氏名・氏名カナの表示用属性
- 判断根拠: User Contextが管理する情報のうち、表示に必要な最小限の情報のみを参照するため

---

# 7. Value Object設計

## AnnouncementStatus

- 採用理由: 「`published`のみを対象とする」という業務ルールを型として明示し、無効な状態文字列の混入を防ぐため
- 独自ルール: `draft` / `scheduled` / `published`のいずれかのみを許容し、本機能では`published`のみを検索条件として扱う
- Entity属性ではなくValue Objectにする理由: 状態の意味と許容値を型として一元管理し、フィルタ条件のロジックを再利用しやすくするため

## AnnouncementAudience（対象範囲条件）

- 採用理由: 所属高校・役割・学年・ユーザー単位という複数の対象条件を、単なる複数引数ではなく一つの概念としてまとめて扱うため
- 独自ルール: 生徒の属性（所属高校・役割・学年・ユーザーID）を入力として、検索条件へ変換するための値のまとまりとして扱う
- Entity属性ではなくValue Objectにする理由: 対象条件の組み合わせロジックを一箇所に集約し、条件追加時の変更範囲を限定するため

## Value Objectを採用しないもの

- title・content: 表示用の文字列そのものであり、独自の業務ルール・比較ロジックを持たないためValue Object化は不要とする

---

# 8. Domain Service

不要と判断する。

理由: 対象範囲判定はEntityの振る舞いというより、検索条件の組み立てそのものである。Rails現行仕様でも`Announcement.for_user`というモデルスコープ（クエリ条件）として実装されており、業務ルールとしての判断ロジックというよりは検索条件の生成に近い。そのため、本機能ではDomain Serviceを設けず、AnnouncementAudienceの値をもとにRepositoryが検索条件を組み立てる責務として扱う。

---

# 9. Repository設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、Repository Interfaceをdomain層に定義しない。以下は永続化・検索責務の設計意図であり、実装時はinfrastructure層の関数として直接実装する(規約: アーキテクチャ規約.md「4. 設計パターンごとの構造適用方針」)。

## AnnouncementRepository

- 管理対象: Announcement
- 責務: 生徒の対象範囲に合致する公開お知らせの一覧・詳細取得
- 保持する検索機能:
  - status（published）による絞り込み
  - AnnouncementAudience（所属高校・役割・学年・ユーザー単位）による対象範囲絞り込み
  - published_at・idの降順ソート
  - ページネーション
  - IDによる単一取得
- 保持しない責務:
  - お知らせの作成・更新・状態遷移
  - 非同期公開処理
- 判断根拠: 参照処理に特化させ、公開・作成といった書き込み責務を持たせないことで、参照系と管理系の責務を明確に分離するため

---

# 10. UseCase設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、UseCase層(struct)を設けない。以下は業務操作の設計意図であり、実装時はapplication層の関数として直接実装する。

## ListAnnouncements

- 目的: 生徒に対する対象お知らせの一覧を取得する
- 入力: current user, page
- 出力: お知らせ一覧とページ情報
- トランザクション範囲: 読み取りのみ、トランザクションは不要
- 呼び出す関数: お知らせ検索関数(AnnouncementRepositoryの設計意図をinfrastructure層の関数として実装)
- 判断根拠: 一覧取得は単純な読み取り処理であり、業務ルールの分岐がないため

## ShowAnnouncement

- 目的: 指定お知らせの詳細を取得する
- 入力: current user, announcement id
- 出力: お知らせ詳細情報
- トランザクション範囲: 読み取りのみ
- 呼び出す関数: お知らせ検索関数
- 判断根拠: 対象範囲外・存在しないお知らせへのアクセスを防ぐため、対象範囲条件を含めた検索が必要なため

---

# 11. Transaction設計

## Transaction開始位置

- なし（参照のみのため使用しない）

## Transaction終了位置

- なし

## 理由

本機能は状態を変更する処理が存在しないため、トランザクションによる整合性保証が不要である。読み取り一貫性が必要な場合でも、単一クエリの範囲で完結するため明示的なトランザクション管理は行わない。

---

# 12. Validation設計

## Presentation

- 型チェック: pageパラメータが数値であることを検証する
- 必須チェック: announcement idの必須確認（詳細取得時）
- フォーマットチェック: idが整数形式であることを検証する

## Domain

- 業務ルール: 対象のお知らせが自分に配信対象となっているか（AnnouncementAudienceに合致するか）
- 状態チェック: statusが`published`であるかどうか
- 整合性チェック: 指定IDのお知らせが存在し、かつ対象範囲内であるか

## 責務分離

- Presentationは「入力値の形式が正しいか」を担当する
- Domainは「そのお知らせが自分に表示してよいものか」という業務的な妥当性を担当する
- これにより、HTTP依存の入力検証と対象範囲判定ロジックを分離できる

---

# 13. Authorization設計

## Middleware

- 認証済みユーザーを特定し、コンテキストへ保持する
- 役割がstudentであることを確認する

## Handler

- APIの入口としてリクエストを受け取り、認証失敗時のHTTP応答を整える
- 対象範囲判定などの業務権限判定は持たせない

## UseCase

- current userの属性をAnnouncementAudienceに変換し、検索関数の条件として利用する（該当するapplication関数内で判断する）
- 対象範囲外のお知らせが取得結果に含まれないことを保証する

## Domain

- Announcement自体は対象範囲を判定する振る舞いを持たない（判定は該当するapplication関数から検索関数への条件指定で完結する）
- 判断根拠: 対象範囲判定は検索条件の組み立てに近く、Entityの責務として持たせるよりも該当するapplication関数・検索関数で一貫して扱う方が単純である

## 判断理由

認証はMiddleware、対象範囲に基づくデータアクセス制御は該当するapplication関数・検索関数で扱うことで、権限ロジックの変更が特定の層に閉じるようにするためである。

---

# 14. Error設計

## Domain Error

責務: 対象範囲外のお知らせへのアクセスなど、業務ルール上許可されないアクセスを表現する

## Application Error

責務: 指定お知らせが存在しない場合や取得処理の失敗を表現する

## Infrastructure Error

責務: DB接続失敗など、永続化層の技術的な障害を表現する

判断理由: 「存在しない（対象外）」という業務上の404相当のケースと、DB接続断のような技術的失敗を明確に分離することで、Handler側でのHTTPステータス変換を単純化できるため。

---

# 15. Domain Event

不要と判断する。

理由: 本機能は参照のみであり、閲覧に伴って他処理へ通知すべき副作用が現行仕様に存在しない。お知らせの公開自体は非同期ジョブ（本機能スコープ外）の責務であり、本機能内でイベントを発火する必要はない。将来的に既読管理や閲覧ログ連携が必要になった場合は、Domain Event化を検討する。

---

# 16. API互換方針

## URL

- GET /api/v1/student/announcements
- GET /api/v1/student/announcements/:id

現行仕様のエンドポイントをそのまま維持する。

## HTTP Method

- 既存仕様どおりGETのみを維持する

## Request

- 一覧: page（任意）を維持する
- 詳細: id（必須）を維持する

## Response

- 一覧: announcements配列（id, title, content, published_at, publisher）+ meta（current_page, total_pages, total_count, per_page）を維持する
- 詳細: id, title, content, published_at, publisherを維持する

## Status Code

- 200: 取得成功
- 404: 対象お知らせが存在しない、または対象範囲外

## Error Response

- 既存のエラーレスポンス形式を踏襲し、フロントエンド互換性を優先する

---

# 17. DB設計方針

## 現行DBを利用するか

- 既存Rails DBを継続利用する

## Schema変更有無

- 変更なし

## 変更理由

- 現行の`announcements`・`announcement_targets`テーブルで本機能の要件を満たしており、参照系機能の移行にスキーマ変更は不要であるため

## 変更を提案しない理由

- 本機能は既存データの参照のみであり、構造的な問題は現行仕様書から確認できないため

---

# 18. テスト戦略

## Domain Test

- 目的: AnnouncementStatus・AnnouncementAudienceの妥当性判定ロジックを検証する

## UseCase Test

- 目的: ListAnnouncementsUseCase・ShowAnnouncementUseCaseが対象範囲外のお知らせを除外することを検証する

## Repository Test

- 目的: AnnouncementRepositoryの検索条件（status・対象範囲・ページング・ソート順）の正確性を検証する

## Handler Test

- 目的: リクエストパラメータの検証結果とHTTPステータスへの変換を検証する

## Integration Test

- 目的: エンドポイント経由で一覧・詳細取得が正常に動作し、対象外お知らせが返却されないことを確認する

---

# 19. Railsとの責務対応

| Rails | Go | 設計方針 |
|---|---|---|
| Controller | Handler | HTTP入力の受け取りとレスポンス整形に限定する |
| Model（`for_user`スコープ） | Repository（検索条件） | 対象範囲判定を検索条件としてRepositoryに集約する |
| Model（status enum） | Value Object（AnnouncementStatus） | 状態の意味と許容値を型として明示する |
| Serializer | Presenter / Response DTO | 画面向けレスポンス整形を分離する |

---

# 20. 採用しなかった設計

## Domain Model

- 採用しなかった理由: 状態遷移は本機能のスコープ外であり、参照処理において複雑な業務ルールが存在しないため
- 将来的に採用する可能性: 生徒側で既読管理などの状態操作機能が追加された場合は、その部分に限定してDomain Model化を検討する

## Active Record

- 採用しなかった理由: 本機能に更新処理が存在しないため、モデルに保存・更新責務を持たせる意義がない
- 将来的に採用する可能性: 生徒側でお知らせに対する操作（既読化等）が追加された場合は再検討の余地がある

## Event Sourcing

- 採用しなかった理由: 履歴の再構築・監査要件が現行仕様に存在しないため
- 将来的に採用する可能性: 閲覧履歴の追跡要件が発生した場合に検討する

---

# 21. 設計判断サマリー

| 項目 | 採用 | 判断理由 |
|---|---|---|
| 設計パターン | Transaction Script | 参照のみで状態変更がなく、対象範囲判定は検索条件の組み立てに帰着するため |
| Aggregate | 未採用 | 書き込みの整合性境界が不要なため |
| Transaction境界 | 未使用 | 状態変更を伴わないため |
| Domain Event | 未採用 | 現時点で他処理への通知要件がない |
| Value Object | 一部採用 | 状態と対象範囲条件のルールを型として明示したいため |
| Authorization | Middleware + 該当するapplication関数・検索関数 | 認証とデータアクセス制御を分離して管理しやすくするため |

---

# 設計差分管理

## Rails現行仕様

- `Announcement.for_user(current_user)`というモデルスコープで対象範囲判定を行っている
- 対象判定ロジックがモデル内のクエリスコープとして実装されている

## Go設計での変更内容

- 対象範囲判定条件をAnnouncementAudienceという値のまとまりとして明示化し、UseCaseからRepositoryへ渡す検索条件として扱う
- 対象範囲判定のロジックをモデル内スコープではなく、Repositoryの検索責務として整理する

## 変更理由

- Railsのモデルスコープはモデルに検索ロジックを集約する設計であり、Goではモデル（Active Record的なEntity）に検索責務を持たせない方針とするため
- 検索条件をRepository層に集約することで、対象判定ロジックの変更影響を限定できるため

## 影響範囲

- フロントエンドから見たAPIの外部仕様（エンドポイント・レスポンス構造）は変更しない
- 既存DBスキーマは維持するため、データ移行は不要

