# コース参照機能 Go移行・設計仕様書

---

# 1. 機能概要

## 機能概要

学習コースとそれに紐づく単元の一覧を取得する参照機能である。Rails現行仕様では、`subject`パラメータによる絞り込みに対応し、コースに含まれる単元情報を合わせて返却する。

## 利用者

- 生徒ユーザー（student ロール）
- 権限制御はなく、全生徒が全コースを参照可能

## 業務上の目的

- 生徒が学習可能なコースとその構成単元を把握できるようにする
- 科目別にコースを絞り込んで表示できるようにする

---

# 2. 設計方針

本機能はマスタデータの参照のみで完結するため、過度な抽象化を避け、シンプルな読み取り専用の構造とする。

- 責務分離: 検索条件の組み立てとレスポンス整形を分離する
- 保守性: コース・単元の属性追加に対して変更範囲を限定する
- テスト容易性: 絞り込み条件ごとの取得結果をUseCase単位で検証できるようにする
- API互換性: 既存エンドポイント・レスポンス構造（コース+units配列）を維持する
- 拡張性: 将来的な単元詳細情報の付加や科目マスタの拡張に備え、検索責務をRepositoryに閉じ込めておく

---

# 3. Bounded Context

## Context名

- curriculum（コース・単元カリキュラム）

## Contextの責務

- コースと単元のマスタデータの保持・参照提供
- 科目による絞り込み

## 他Contextとの依存関係

- 特になし（強い依存関係はない）
- 科目（subject）情報はコースの属性として本Context内で保持する想定とする（推測: Rails現行仕様書に`subjects`テーブルの詳細記載がないため、コースの属性の一部として扱う）

## 依存する理由

本機能は権限制御を持たず、コース・単元というマスタデータの参照のみで完結するため、他Contextへの機能的な依存は発生しない。

---

# 4. 設計パターン

## 採用パターン

Transaction Script

## 判断根拠

本機能は一覧取得（科目による絞り込みを含む）のみで構成される参照系機能であり、状態管理・権限制御・業務ルールのいずれも現行仕様に存在しない。CourseとUnitは単純なマスタデータであり、更新や状態遷移を伴わない。

- 業務ルール: 「subjectが指定されればその科目のコースのみ返す」という単純な条件分岐のみ
- 状態管理: 存在しない
- 将来拡張: 将来的に単元の詳細情報や進捗情報が付加される場合でも、参照ロジック自体は単純な検索条件の追加で対応でき、Transaction Scriptのまま拡張可能
- テスト容易性: 入力条件（subjectの有無）に対する出力を検証するだけでよく、Entityの状態遷移を検証する必要がない

## 採用しなかったパターン

### Active Record

コース・単元に対する作成・更新処理が本機能に存在しないため、モデルに保存・更新責務を持たせる意味がない。

### Domain Model

コース・単元は状態も振る舞いも持たない完全なマスタ参照データであり、Entityに業務ルールを集約するメリットがない。

### Event Sourcing

履歴管理・監査要件が現行仕様に存在しない。

---

# 5. Aggregate設計

不要と判断する。

理由: 更新を伴わない参照専用データであり、整合性を保証すべき書き込み境界が存在しない。CourseとUnitは1対多の単純な参照関係として扱う。

---

# 6. Entity設計

## Course

- 役割: 学習コースのマスタ情報を表す概念
- ライフサイクル: 本機能では扱わない（参照専用）
- 状態変化: なし
- 保持する責務: subject_id・level_number・level_name・descriptionなどの属性保持
- 判断根拠: 生徒が参照するコース情報の中心的な概念であるため

## Unit

- 役割: コースに含まれる単元のマスタ情報を表す概念
- ライフサイクル: 本機能では扱わない（参照専用）
- 状態変化: なし
- 保持する責務: course_id・unit_nameなどの属性保持
- 判断根拠: コースを構成する単元情報として、一覧取得時に合わせて返却する必要があるため

---

# 7. Value Object設計

不要と判断する。

理由: level_number・level_name・description・unit_nameなどの属性は単純な値であり、独自の比較ロジックや業務ルールを持たない。subjectによる絞り込みも単純な等価条件であり、Value Object化によるメリットが薄い。将来的にsubjectが複雑な階層構造を持つようになった場合は、Value Object化を再検討する余地がある。

---

# 8. Domain Service

不要と判断する。

理由: 本機能には業務ルールと呼べる判定処理が存在せず、subjectによる検索条件の組み立てのみで処理が完結するため。

---

# 9. Repository設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、Repository Interfaceをdomain層に定義しない。以下は永続化・検索責務の設計意図であり、実装時はinfrastructure層の関数として直接実装する(規約: アーキテクチャ規約.md「4. 設計パターンごとの構造適用方針」)。

## CourseRepository

- 管理対象: Course（関連するUnitを含む）
- 責務: コース一覧の取得、単元情報を含めた取得、科目による絞り込み
- 保持する検索機能:
  - subject_idによる絞り込み
  - 関連するUnitの一括取得（N+1を避けるための取得方法を含む）
- 保持しない責務: コース・単元の作成・更新・削除
- 判断根拠: 参照処理に特化させ、業務ロジックを持たせないため

---

# 10. UseCase設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、UseCase層(struct)を設けない。以下は業務操作の設計意図であり、実装時はapplication層の関数として直接実装する。

## ListCourses

- 目的: コースと単元一覧を取得する
- 入力: subject（任意）
- 出力: コース一覧（各コースに紐づく単元を含む）
- トランザクション範囲: 読み取りのみ、トランザクションは不要
- 呼び出す関数: コース検索関数(CourseRepositoryの設計意図をinfrastructure層の関数として実装)
- 判断根拠: 単一の検索条件による一覧取得であり、複数の検索関数を組み合わせる必要がないため

---

# 11. Transaction設計

## Transaction開始位置

- なし

## Transaction終了位置

- なし

## 理由

状態を変更する処理が存在しないため、トランザクションによる整合性保証は不要である。

---

# 12. Validation設計

## Presentation

- 型チェック: subjectが文字列であることを検証する
- 必須チェック: なし（subjectは任意項目）
- フォーマットチェック: 特になし

## Domain

- 業務ルール: 特になし
- 状態チェック: 特になし
- 整合性チェック: 特になし

## 責務分離

本機能は業務ルールを持たないため、Presentation層での型チェックのみで入力検証が完結する。Domain側で追加の妥当性判定は行わない。

---

# 13. Authorization設計

## Middleware

- 認証済みユーザーであることの確認のみ行う

## Handler

- 業務権限判定は持たせない

## UseCase

- 権限判定は行わない（全コースが参照対象のため）

## Domain

- 権限判定は行わない

## 判断理由

Rails現行仕様書においても「権限制御: なし（すべてのコースを取得可能）」と明記されており、本機能はコースマスタの公開参照機能であるため、認証確認以外の権限制御を設けない。

---

# 14. Error設計

## Domain Error

責務: 本機能では業務ルール違反が発生しないため、原則として使用しない

## Application Error

責務: 取得処理自体の失敗（想定外の検索条件など）を表現する

## Infrastructure Error

責務: DB接続失敗など、永続化層の技術的な障害を表現する

判断理由: 本機能は単純な参照処理であり、業務エラーがほぼ発生しないため、Application Error・Infrastructure Errorを中心としたシンプルなエラー設計とする。

---

# 15. Domain Event

不要と判断する。

理由: 参照のみの機能であり、他処理への通知が必要な副作用が現行仕様に存在しないため。

---

# 16. API互換方針

## URL

- GET /api/v1/student/courses

現行仕様のエンドポイントを維持する。

## HTTP Method

- 既存仕様どおりGETのみを維持する

## Request

- subject（任意）を維持する

## Response

- コース属性（id, subject_id, level_number, level_name, description）+ units（id, course_id, unit_name）を維持する

## Status Code

- 200: 取得成功

## Error Response

- 本機能では業務エラーがほぼ発生しないため、想定外エラー時のみ既存形式に準じたレスポンスを返す

---

# 17. DB設計方針

## 現行DBを利用するか

- 既存Rails DBを継続利用する

## Schema変更有無

- 変更なし

## 変更理由

- 現行の`courses`・`units`テーブルで本機能の要件を満たしているため

## 変更を提案しない理由

- 参照のみの機能であり、現行スキーマに構造的な問題は確認できないため

---

# 18. テスト戦略

## Domain Test

- 対象なし（業務ルールが存在しないため、Domain層の単体テストは最小限とする）

## UseCase Test

- 目的: subjectの有無によるコース一覧取得結果の違いを検証する

## Repository Test

- 目的: CourseRepositoryの検索条件（subject絞り込み・単元の同時取得）の正確性を検証する

## Handler Test

- 目的: リクエストパラメータの検証結果とレスポンス形式を検証する

## Integration Test

- 目的: エンドポイント経由でコース一覧・単元情報が正しく取得できることを確認する

---

# 19. Railsとの責務対応

| Rails | Go | 設計方針 |
|---|---|---|
| Controller | Handler | HTTP入力の受け取りとレスポンス整形に限定する |
| Query（CoursesQuery） | Repository（検索条件） | 検索条件の組み立てをRepositoryに集約する |
| Model | Entity + Repository | 属性保持はEntity、永続化・検索はRepositoryとして整理する |
| as_json | Presenter / Response DTO | レスポンス整形を明示的な層として分離する |

---

# 20. 採用しなかった設計

## Domain Model

- 採用しなかった理由: コース・単元が状態も振る舞いも持たない完全なマスタ参照データであるため
- 将来的に採用する可能性: 将来的にコースに複雑な進捗ルールや公開制御ルールが追加された場合は再検討する

## Active Record

- 採用しなかった理由: 更新処理が存在しないため、モデルに保存・更新責務を持たせる意義がない
- 将来的に採用する可能性: 管理者向けのコース編集機能が同一Contextで実装される場合は再検討の余地がある

## Event Sourcing

- 採用しなかった理由: 履歴・監査要件が現行仕様に存在しないため
- 将来的に採用する可能性: 現時点では想定していない

---

# 21. 設計判断サマリー

| 項目 | 採用 | 判断理由 |
|---|---|---|
| 設計パターン | Transaction Script | 状態管理・権限制御・業務ルールがない単純な参照機能のため |
| Aggregate | 未採用 | 書き込みの整合性境界が不要なため |
| Transaction境界 | 未使用 | 状態変更を伴わないため |
| Domain Event | 未採用 | 他処理への通知要件がない |
| Value Object | 未採用 | 属性が単純な値であり、独自ルールを持たないため |
| Authorization | 認証確認のみ | 権限制御自体が現行仕様に存在しないため |

---

# 設計差分管理

## Rails現行仕様

- `CoursesQuery`というQuery Objectで検索条件を組み立てている
- `as_json(include: :units)`でレスポンスを整形している

## Go設計での変更内容

- Query Objectを個別に設けず、CourseRepositoryの検索責務として統合する
- レスポンス整形をPresenter / Response DTOとして明示的な層に分離する

## 変更理由

- 本機能の検索条件は単純であり、Query Objectを独立させるほどの複雑さがないため、Repositoryに統合することでレイヤーを簡潔に保つ
- レスポンス整形を明示的な層として分離することで、APIレスポンス構造の変更がドメイン層に影響しないようにするため

## 影響範囲

- フロントエンドから見たAPIの外部仕様（エンドポイント・レスポンス構造）は変更しない
- 既存DBスキーマは維持するため、データ移行は不要

