# ダッシュボード機能 Go移行・設計仕様書

---

# 1. 機能概要

## 機能概要

生徒のホーム画面に表示する、期限が近い目標（Goal）を最大5件取得する参照機能である。Rails現行仕様では、生徒自身の目標を`due_date`昇順で取得し、上位5件に絞り込んで返却する。

## 利用者

- 生徒ユーザー（student ロール）
- 自分自身の目標のみ対象

## 業務上の目的

- 生徒がホーム画面から直近の学習目標を素早く把握できるようにする
- 期限が近い目標を優先的に表示し、学習計画の抜け漏れを防ぐ

---

# 2. 設計方針

本機能は単一の集計クエリで完結する参照機能であるため、シンプルな構造を保ちつつ、将来的なダッシュボード項目の追加に耐えられる拡張性を確保する。

- 責務分離: 集計条件の組み立てとレスポンス整形を分離する
- 保守性: 表示件数・並び順といった業務ルールの変更に対して影響範囲を限定する
- テスト容易性: UseCase単位で取得件数・並び順を検証できるようにする
- API互換性: 既存エンドポイント・レスポンス構造を維持する
- 拡張性: 将来的に他の集計項目（タスク進捗など）が追加された場合、独立したUseCaseとして追加できる構造とする

---

# 3. Bounded Context

## Context名

- dashboard（生徒向けダッシュボード）

## Contextの責務

- 生徒向けサマリー情報（期限が近い目標など）の集約・提供

## 他Contextとの依存関係

- goal-management: 生徒の目標データの参照に依存する

## 依存する理由

ダッシュボードは目標データそのものを所有・管理するのではなく、他Context（goal-management）が管理するデータを読み取り専用で集約して表示する性質の機能である。目標の作成・更新・状態遷移といった業務ルールはgoal-managementの責務であり、本Contextは表示のための参照に徹する。

---

# 4. 設計パターン

## 採用パターン

Transaction Script

## 判断根拠

本機能は「生徒自身の目標をdue_date昇順で取得し、上位5件に絞り込む」という単一の集計処理のみで構成されており、状態管理・状態遷移・複雑な業務ルールが存在しない。

- 業務ルール: 「期限が近い順で最大5件」という単純な並び替え・件数制限のみ
- 状態管理: 本機能側では目標の状態を変更しない（読み取り専用）
- 将来拡張: 将来的に他の集計項目（タスク進捗率など）が追加される場合も、それぞれ独立したUseCase（Transaction Script）として追加すればよく、既存のロジックに影響を与えない
- テスト容易性: 入力（現在の生徒）に対する出力（上位5件の目標）を検証するだけでよい

## 採用しなかったパターン

### Active Record

本機能に目標の作成・更新処理は含まれないため、モデルに保存・更新責務を持たせる意味がない。

### Domain Model

目標の状態遷移や業務ルールはgoal-management側の責務であり、本機能（ダッシュボード）側では目標データを読み取り専用で参照するのみである。ダッシュボード固有のEntityに振る舞いを持たせる必要がない。

### Event Sourcing

本機能に履歴の再構築・監査要件は存在しない。

---

# 5. Aggregate設計

不要と判断する。

理由: 本機能は他Contextが管理するデータを読み取り専用で集約表示するのみであり、書き込みの整合性を保証する境界が存在しない。

---

# 6. Entity設計

本機能では独自のEntityを新規に定義しない。

## Goal（参照専用）

- 役割: 生徒の学習目標を表す概念。本機能ではgoal-managementが管理する目標情報を読み取り専用で参照する
- ライフサイクル・状態変化: 本機能では扱わない（goal-management側の責務）
- 保持する責務: 表示に必要な属性（id, title, description, due_date, status）の取得
- 判断根拠: 目標の所有・状態管理はgoal-managementの責務であり、ダッシュボードは表示用の参照に徹するため

---

# 7. Value Object設計

不要と判断する。

理由: due_dateによる並び替え・上位5件への絞り込みは標準的な比較・件数制限であり、独自の業務ルールを持つ値として切り出す必要がない。

---

# 8. Domain Service

不要と判断する。

理由: 「due_date昇順で上位5件」という単純な集計ロジックのみであり、複数Entityにまたがる業務ルールの判定が存在しないため。

---

# 9. Repository設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、Repository Interfaceをdomain層に定義しない。以下は永続化・検索責務の設計意図であり、実装時はinfrastructure層の関数として直接実装する(規約: アーキテクチャ規約.md「4. 設計パターンごとの構造適用方針」)。

## GoalSummaryRepository

- 管理対象: Goal（ダッシュボード表示用の参照）
- 責務: 生徒の目標をdue_date昇順で取得し、上位5件に絞り込んで取得する
- 保持する検索機能:
  - user_idによる絞り込み
  - due_date昇順ソート
  - 件数制限（上位5件）
- 保持しない責務: 目標の作成・更新・削除、状態遷移の判断
- 判断根拠: ダッシュボード表示に特化した参照専用の関数として実装することで、goal-management本体の永続化責務と分離するため

---

# 10. UseCase設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、UseCase層(struct)を設けない。以下は業務操作の設計意図であり、実装時はapplication層の関数として直接実装する。

## GetDashboard

- 目的: 生徒の期限が近い目標を最大5件取得する
- 入力: current user
- 出力: 目標一覧（最大5件）
- トランザクション範囲: 読み取りのみ、トランザクションは不要
- 呼び出す関数: 目標サマリー取得関数(GoalSummaryRepositoryの設計意図をinfrastructure層の関数として実装)
- 判断根拠: 単一の集計クエリのみで完結し、複数の業務ルールを組み合わせる必要がないため

---

# 11. Transaction設計

## Transaction開始位置

- なし

## Transaction終了位置

- なし

## 理由

状態を変更する処理が存在しないため、トランザクションによる整合性保証は不要である。

---

# 12. Validation設計

## Presentation

- 型チェック: リクエストパラメータが存在しないため、実質的な検証項目はない
- 必須チェック: なし
- フォーマットチェック: なし

## Domain

- 業務ルール: 特になし
- 状態チェック: 特になし
- 整合性チェック: 特になし

## 責務分離

本機能はリクエストパラメータを持たない単純な参照機能であるため、入力検証はほぼ不要である。現在の認証ユーザーの識別のみが前提条件となる。

---

# 13. Authorization設計

## Middleware

- 認証済みユーザーを特定し、コンテキストへ保持する
- 役割がstudentであることを確認する

## Handler

- 業務権限判定は持たせない

## UseCase

- current userのuser_idを目標サマリー取得関数の検索条件に渡すことで、自分自身の目標のみ取得できるようにする

## Domain

- 権限判定は行わない（該当するapplication関数側の検索条件で担保する）

## 判断理由

本機能はcurrent userのスコープに閉じた単純な参照であるため、認証確認と該当するapplication関数での検索条件スコープ化のみで十分であり、追加の認可レイヤーを設ける必要はない。

---

# 14. Error設計

## Domain Error

責務: 本機能では業務ルール違反がほぼ発生しないため、原則として使用しない

## Application Error

責務: 取得処理自体の失敗を表現する

## Infrastructure Error

責務: DB接続失敗など、永続化層の技術的な障害を表現する

判断理由: 本機能は単純な参照処理であり、業務エラーが発生しないため、Application Error・Infrastructure Errorを中心としたシンプルなエラー設計とする。

---

# 15. Domain Event

不要と判断する。

理由: 参照のみの機能であり、表示に伴って他処理へ通知すべき副作用が現行仕様に存在しないため。

---

# 16. API互換方針

## URL

- GET /api/v1/student/dashboard

現行仕様のエンドポイントを維持する。

## HTTP Method

- 既存仕様どおりGETのみを維持する

## Request

- パラメータなし

## Response

- 目標リスト（id, title, description, status, due_date）を維持する

## Status Code

- 200: 取得成功

## Error Response

- 本機能では業務エラーがほぼ発生しないため、想定外エラー時のみ既存形式に準じたレスポンスを返す

---

# 17. DB設計方針

## 現行DBを利用するか

- 既存Rails DBを継続利用する

## Schema変更有無

- 変更なし

## 変更理由

- 現行の`goals`テーブルで本機能の要件を満たしているため

## 変更を提案しない理由

- 参照のみの機能であり、現行スキーマに構造的な問題は確認できないため

---

# 18. テスト戦略

## Domain Test

- 対象なし（業務ルールが存在しないため、Domain層の単体テストは最小限とする）

## UseCase Test

- 目的: GetDashboardUseCaseが自分自身の目標のみを、due_date昇順・上位5件で返すことを検証する

## Repository Test

- 目的: GoalSummaryRepositoryの検索条件（user_id絞り込み・ソート順・件数制限）の正確性を検証する

## Handler Test

- 目的: 認証済みユーザーに対して正しくレスポンスが返ることを検証する

## Integration Test

- 目的: エンドポイント経由でダッシュボードの目標一覧が正しく取得できることを確認する

---

# 19. Railsとの責務対応

| Rails | Go | 設計方針 |
|---|---|---|
| Controller | Handler | HTTP入力の受け取りとレスポンス整形に限定する |
| Query（GoalsQuery） | Repository（検索条件） | 集計条件の組み立てをRepositoryに集約する |
| Serializer | Presenter / Response DTO | レスポンス整形を明示的な層として分離する |
| Model（Goal） | 参照用Entity（goal-management由来） | 目標の所有・管理はgoal-managementに委ね、本Contextは参照に徹する |

---

# 20. 採用しなかった設計

## Domain Model

- 採用しなかった理由: ダッシュボードは他Contextのデータを読み取り専用で集約表示するのみであり、独自の業務ルールを持たないため
- 将来的に採用する可能性: 現時点では想定していない

## Active Record

- 採用しなかった理由: 更新処理が存在しないため、モデルに保存・更新責務を持たせる意義がない
- 将来的に採用する可能性: 現時点では想定していない

## Event Sourcing

- 採用しなかった理由: 履歴・監査要件が現行仕様に存在しないため
- 将来的に採用する可能性: 現時点では想定していない

---

# 21. 設計判断サマリー

| 項目 | 採用 | 判断理由 |
|---|---|---|
| 設計パターン | Transaction Script | 単一の集計クエリのみで完結し、状態管理・業務ルールがないため |
| Aggregate | 未採用 | 他Contextデータの読み取り専用集約であり、整合性境界が不要なため |
| Transaction境界 | 未使用 | 状態変更を伴わないため |
| Domain Event | 未採用 | 他処理への通知要件がない |
| Value Object | 未採用 | 標準的な比較・件数制限のみで独自ルールを持たないため |
| Authorization | Middleware + 該当するapplication関数 | 認証確認とcurrent userスコープの検索条件で十分なため |

---

# 設計差分管理

## Rails現行仕様

- `current_user.goals`に`GoalsQuery`（Query Object）の`due_soon.limit_five`を適用している
- `Student::GoalSerializer`でレスポンスを整形している

## Go設計での変更内容

- Query Objectを個別に設けず、GoalSummaryRepositoryの検索責務として統合する
- レスポンス整形をPresenter / Response DTOとして明示的な層に分離する

## 変更理由

- 本機能の集計条件は単純であり、Query Objectを独立させるほどの複雑さがないため、Repositoryに統合することでレイヤーを簡潔に保つ
- ダッシュボード表示専用の参照Repositoryとすることで、goal-management本体の永続化責務と混在させないため

## 影響範囲

- フロントエンドから見たAPIの外部仕様（エンドポイント・レスポンス構造）は変更しない
- 既存DBスキーマは維持するため、データ移行は不要

