# 単元参照機能 Go移行・設計仕様書

---

# 1. 機能概要

## 機能概要

生徒が自分のタスクに紐づく単元の詳細情報を取得する参照機能である。Rails現行仕様では、`current_user.tasks.find(task_id)`によってタスクの所有権を確認したうえで、そのタスクに紐づく単元をIDで検索し、コース情報を含めて返却する。

## 利用者

- 生徒ユーザー（student ロール）
- 自分が所有するタスクに紐づく単元のみ取得可能

## 業務上の目的

- タスク画面から、紐づく単元の詳細（単元名・所属コース情報）を確認できるようにする
- タスクに紐づかない単元への不正なアクセスを防ぐ

---

# 2. 設計方針

本機能は単一の参照処理であるが、タスクの所有権確認と単元の紐付き確認という2段階のアクセス制御を伴う。Go設計では、この確認ロジックを明示的なUseCaseの手続きとして表現し、Railsのようにモデルの関連スコープに暗黙的に埋め込まない方針とする。

- 責務分離: 所有権確認・紐付き確認・レスポンス整形を分離する
- 保守性: アクセス制御の条件が変わった場合の影響範囲を限定する
- テスト容易性: 所有権のないタスク・紐づかない単元へのアクセスがエラーになることをUseCase単位で検証できるようにする
- API互換性: 既存エンドポイント・レスポンス構造を維持する
- 拡張性: 将来的に単元の学習進捗情報などが付加される場合に備え、参照ロジックを独立させておく

---

# 3. Bounded Context

## Context名

- curriculum（コース・単元カリキュラム）

コース参照機能（`コース参照機能_Go移行・設計仕様書`）と同一のContextとして扱う。両機能はいずれもCourse・Unitというマスタデータを参照する機能であり、Context責務を分割する理由がないためである。

## Contextの責務

- コースと単元のマスタデータの保持・参照提供（コース参照機能と共通）
- タスクとの紐付きを前提とした単元の安全な参照提供

## 他Contextとの依存関係

- task-management: 指定タスクが現在の生徒に属するか、また指定単元がそのタスクに紐づいているかの確認に依存する

## 依存する理由

本機能はタスクという他Contextの概念を起点としたアクセス制御を伴う。単元自体はcurriculum Contextが管理するが、「どのタスクに紐づく単元を取得してよいか」はtask-managementが管理するタスクと単元の関連情報に依存するため、task-managementへの依存が発生する。

---

# 4. 設計パターン

## 採用パターン

Transaction Script

## 判断根拠

本機能は「タスクの所有権確認→紐づく単元の検索→返却」という一連の手続きのみで構成される参照系機能であり、状態管理・状態遷移は存在しない。単元自体はマスタデータであり、業務ルールを持って振る舞うEntityではない。

- 業務ルール: 「タスクに紐づかない単元は取得できない」という単純なアクセス制御ルールのみ
- 状態管理: 存在しない（参照専用）
- 将来拡張: 将来的に単元に学習進捗情報が付加される場合も、参照手続きに取得項目を追加するだけで対応でき、Transaction Scriptのまま拡張可能
- テスト容易性: 「所有タスクに紐づく単元」「所有しないタスク」「紐づかない単元」という入力パターンごとに出力を検証すればよい

## 採用しなかったパターン

### Active Record

単元に対する作成・更新処理が本機能に存在しないため、モデルに保存・更新責務を持たせる意味がない。

### Domain Model

単元は状態も振る舞いも持たない参照専用のマスタデータであり、アクセス制御ルールも「タスクとの紐付きの有無」という単純な条件判定にとどまる。Entityに業務ルールを集約するメリットが小さい。

### Event Sourcing

履歴管理・監査要件が現行仕様に存在しない。

---

# 5. Aggregate設計

不要と判断する。

理由: 本機能は参照のみであり、書き込みの整合性を保証すべき境界が存在しない。Task・Unit・Courseはそれぞれ独立した参照対象として扱う。

---

# 6. Entity設計

## Unit

- 役割: タスクに紐づく単元情報を表す概念
- ライフサイクル: 本機能では扱わない（参照専用）
- 状態変化: なし
- 保持する責務: course_id・unit_nameなどの属性保持
- 判断根拠: 生徒が参照する単元情報の中心的な概念であるため

## Course（参照情報）

- 役割: 単元が属するコースの表示用情報
- ライフサイクル・状態変化: 本機能では扱わない
- 保持する責務: id・level_number・level_nameの表示用属性
- 判断根拠: 単元の所属コース情報として、詳細画面表示に必要な最小限の情報を提供するため

## Task（参照専用、アクセス制御のためのみ利用）

- 役割: 対象単元が紐づくタスクの所有権を確認するための参照情報
- ライフサイクル・状態変化: 本機能では扱わない（task-managementの責務）
- 保持する責務: 所有者（生徒）情報と、紐づく単元IDの確認
- 判断根拠: 本機能における単元へのアクセス可否は、タスクの所有権に依存するため、確認のためだけに参照する

---

# 7. Value Object設計

不要と判断する。

理由: unit_name・level_number・level_nameといった属性は単純な値であり、独自の比較ロジックや業務ルールを持たない。タスクと単元の紐付き確認も、IDの一致判定という単純な条件であり、Value Object化によるメリットが薄い。

---

# 8. Domain Service

不要と判断する。

理由: アクセス可否の判定は「タスクの所有者が自分であるか」「単元がそのタスクに紐づいているか」という2段階の単純な条件判定に還元でき、複数Entityにまたがる複雑な業務ルールではない。そのため、UseCase内の手続き、またはRepositoryの検索条件として扱い、独立したDomain Serviceは設けない。

---

# 9. Repository設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、Repository Interfaceをdomain層に定義しない。以下は永続化・検索責務の設計意図であり、実装時はinfrastructure層の関数として直接実装する(規約: アーキテクチャ規約.md「4. 設計パターンごとの構造適用方針」)。

## TaskRepository

- 管理対象: Task（task-management由来、本機能では所有権確認のみに利用）
- 責務: 指定タスクIDが現在の生徒に属するかどうかの確認
- 保持する検索機能: task_id + user_idによる所有権確認検索
- 保持しない責務: タスクの作成・更新・単元紐付けの変更
- 判断根拠: タスクの永続化責務はtask-managementにあり、本機能では所有権確認のための参照のみを行うため

## UnitRepository

- 管理対象: Unit（Courseを含む）
- 責務: 指定タスクに紐づく単元の取得（コース情報を含む）
- 保持する検索機能: task_id + unit_idの組み合わせによる紐付き確認検索、コース情報の同時取得
- 保持しない責務: 単元の作成・更新・削除、タスクとの紐付け自体の変更
- 判断根拠: 単元の永続化責務はcurriculum Context本体にあり、本機能ではタスクとの紐付きを条件とした参照のみを行うため

---

# 10. UseCase設計

**実装上の位置づけ**: 本機能はTransaction Script採用のため、UseCase層(struct)を設けない。以下は業務操作の設計意図であり、実装時はapplication層の関数として直接実装する。

## ShowTaskUnit

- 目的: タスクに紐づく単元の詳細を取得する
- 入力: current user, task_id, unit_id
- 出力: 単元詳細情報（コース情報を含む）
- トランザクション範囲: 読み取りのみ、トランザクションは不要
- 呼び出す関数: タスク所有権確認関数（TaskRepositoryの設計意図をinfrastructure層の関数として実装）, 単元取得関数（UnitRepositoryの設計意図をinfrastructure層の関数として実装。タスクとの紐付き確認込み）
- 判断根拠: タスクの所有権確認と単元の紐付き確認という2段階のアクセス制御を、application関数内で明示的な手順として表現するため

---

# 11. Transaction設計

## Transaction開始位置

- なし

## Transaction終了位置

- なし

## 理由

状態を変更する処理が存在しないため、トランザクションによる整合性保証は不要である。

---

# 12. Validation設計

## Presentation

- 型チェック: task_id・idが整数であることを検証する
- 必須チェック: task_id・idの両方が必須項目であることを検証する
- フォーマットチェック: 特になし

## Domain

- 業務ルール: 指定タスクが自分の所有であるか
- 状態チェック: 特になし
- 整合性チェック: 指定単元が指定タスクに紐づいているか

## 責務分離

- Presentationは「task_id・idが正しい形式で指定されているか」を担当する
- Domainは「そのタスク・単元の組み合わせが自分にとって参照可能なものか」という業務的な妥当性を担当する
- これにより、Railsのモデル関連スコープ（`current_user.tasks.find`）に暗黙的に埋め込まれていたアクセス制御ロジックを、明示的な業務ルールとして分離する

---

# 13. Authorization設計

## Middleware

- 認証済みユーザーを特定し、コンテキストへ保持する
- 役割がstudentであることを確認する

## Handler

- 業務権限判定は持たせない

## UseCase

- current userのuser_idを用いて、指定タスクが自分の所有であることをタスク所有権確認関数で確認する
- 所有確認後、そのタスクに紐づく単元のみを単元取得関数で取得する

## Domain

- Unit自体はアクセス制御の振る舞いを持たない（判定は該当するapplication関数から検索関数への条件指定で完結する）
- 判断根拠: アクセス可否判定はタスクとの紐付きという関係性の確認であり、Unit Entity単体の責務として持たせるよりも該当するapplication関数で一貫して扱う方が単純である

## 判断理由

認証はMiddleware、タスク所有権・単元紐付きに基づくアクセス制御は該当するapplication関数・検索関数で扱うことで、Railsのように関連スコープの副作用としてアクセス制御が行われる状態を避け、業務ルールとして明示的にテスト可能にするためである。

---

# 14. Error設計

## Domain Error

責務: タスクに紐づかない単元へのアクセスなど、業務ルール上許可されないアクセスを表現する

## Application Error

責務: 指定タスクまたは指定単元が存在しない場合の取得処理の失敗を表現する

## Infrastructure Error

責務: DB接続失敗など、永続化層の技術的な障害を表現する

判断理由: 「タスクが存在しない・自分のものでない」「単元がタスクに紐づかない」というケースをまとめて404として扱う現行仕様の挙動を維持しつつ、原因ごとにエラーの種類を分離することで、内部的な原因追跡をしやすくするため。

---

# 15. Domain Event

不要と判断する。

理由: 参照のみの機能であり、単元詳細の閲覧に伴って他処理へ通知すべき副作用が現行仕様に存在しないため。

---

# 16. API互換方針

## URL

- GET /api/v1/student/tasks/:task_id/units/:id

現行仕様のエンドポイントを維持する。

## HTTP Method

- 既存仕様どおりGETのみを維持する

## Request

- task_id（必須）, id（必須）を維持する

## Response

- id, course_id, unit_name, course（id, level_number, level_name）を維持する

## Status Code

- 200: 取得成功
- 404: 対象タスクまたは対象単元が存在しない

## Error Response

- 既存のエラーレスポンス形式（「対象データが見つかりません」）を踏襲する

---

# 17. DB設計方針

## 現行DBを利用するか

- 既存Rails DBを継続利用する

## Schema変更有無

- 変更なし

## 変更理由

- 現行の`units`・`courses`・`tasks`・`task_units`テーブルで本機能の要件を満たしているため

## 変更を提案しない理由

- 参照のみの機能であり、現行スキーマに構造的な問題は確認できないため

---

# 18. テスト戦略

## Domain Test

- 対象なし（業務ルールは所有権・紐付き確認のみであり、UseCase Testでカバーする）

## UseCase Test

- 目的: 自分が所有するタスクに紐づく単元のみ取得できること、所有しないタスク・紐づかない単元の場合にエラーとなることを検証する

## Repository Test

- 目的: TaskRepositoryの所有権確認検索、UnitRepositoryの紐付き確認検索の正確性を検証する

## Handler Test

- 目的: リクエストパラメータの検証結果とHTTPステータスへの変換を検証する

## Integration Test

- 目的: エンドポイント経由でタスクに紐づく単元詳細が正しく取得でき、不正なアクセスに対して404が返ることを確認する

---

# 19. Railsとの責務対応

| Rails | Go | 設計方針 |
|---|---|---|
| Controller | Handler | HTTP入力の受け取りとレスポンス整形に限定する |
| Model関連スコープ（`current_user.tasks.find`） | application関数内の明示的な所有権確認手順 | 暗黙的な関連スコープに埋め込まれたアクセス制御を、明示的な業務手順として分離する(Transaction Script採用のためusecase層は設けない) |
| Serializer | Presenter / Response DTO | レスポンス整形を明示的な層として分離する |

---

# 20. 採用しなかった設計

## Domain Model

- 採用しなかった理由: 単元が状態も振る舞いも持たない参照専用のマスタデータであり、アクセス制御ルールも単純な条件判定にとどまるため
- 将来的に採用する可能性: 単元に学習進捗などの状態が追加され、複雑な業務ルールが発生した場合は再検討する

## Active Record

- 採用しなかった理由: 更新処理が存在しないため、モデルに保存・更新責務を持たせる意義がないため
- 将来的に採用する可能性: 現時点では想定していない

## Event Sourcing

- 採用しなかった理由: 履歴・監査要件が現行仕様に存在しないため
- 将来的に採用する可能性: 現時点では想定していない

---

# 21. 設計判断サマリー

| 項目 | 採用 | 判断理由 |
|---|---|---|
| 設計パターン | Transaction Script | 所有権確認と紐付き確認のみで完結する単純な参照機能のため |
| Aggregate | 未採用 | 書き込みの整合性境界が不要なため |
| Transaction境界 | 未使用 | 状態変更を伴わないため |
| Domain Event | 未採用 | 他処理への通知要件がない |
| Value Object | 未採用 | 属性・条件判定が単純であり、独自ルールを持たないため |
| Authorization | Middleware + 該当するapplication関数 | 所有権確認・紐付き確認を業務手順として明示的に扱うため |

---

# 設計差分管理

## Rails現行仕様

- `current_user.tasks.find(task_id)`というモデルの関連スコープを用いて、タスクの所有権確認を暗黙的に行っている
- 所有権のないタスクIDを指定した場合、関連スコープの絞り込みの結果として`RecordNotFound`が発生し404となる

## Go設計での変更内容

- タスクの所有権確認をUseCase内の明示的な手順として分離する
- 単元の紐付き確認をUnitRepositoryの検索条件として明示化する

## 変更理由

- Railsの関連スコープによる暗黙的なアクセス制御は、クエリの副作用としてアクセス制御が行われるため、ロジックの意図が読み取りにくい
- Go設計では、所有権確認・紐付き確認を明示的な業務手順として表現することで、可読性とテスト容易性を向上させる

## 影響範囲

- フロントエンドから見たAPIの外部仕様（エンドポイント・レスポンス構造・404の挙動）は変更しない
- 既存DBスキーマは維持するため、データ移行は不要

