# Go コーディング規約

## 1. 基本方針

* Go 標準の書き方を優先する
* 独自ルールを増やしすぎない
* `gofmt` による自動フォーマットを必須とする
* 可読性を優先し、過度な抽象化を避ける
* エラーを無視しない
* 不要なコメント・不要なコードを増やさない

---

## 2. コードフォーマット

基本的に手動でフォーマットしない。

以下のいずれかを使用する。

```bash
# 単一ファイルをフォーマット
go fmt main.go

# パッケージ全体をフォーマット
go fmt ./...

# gofmt を使用
gofmt -w *.go

# goimports を使用（インポートを自動管理）
goimports -w *.go
```

すべての Go コードは `gofmt` でフォーマットする。

### Good

```bash
gofmt -w *.go
```

### Bad

```go
// 手動でインデントや空白を調整したコードをそのままコミットする
func main() {
fmt.Println("Hello")
}
```

---

## 3. 命名規則

Go では大文字・小文字によって公開・非公開を表現する。

### パッケージ名

小文字の単語で簡潔にする。

### Good

```go
package httputil
```

### Bad

```go
package HTTPUtil
```

`util` や `common` のような一般的すぎるパッケージ名は避ける。

### Good

```go
package httputil
```

### Bad

```go
package util
```

```go
package common
```

### 公開・非公開

公開する識別子は先頭を大文字にする。

```go
// Good

// 公開関数
func NewClient() *Client {}

// 非公開関数
func parseURL(url string) error {}
```

```go
// Bad

// 公開する関数を小文字で定義している
func newClient() *Client {}

// 非公開関数を大文字で定義している
func ParseURL(url string) error {}
```

### 定数

キャメルケースを使用し、アンダースコアは使用しない。

```go
// Good
const MaxRetryCount = 3
const defaultTimeout = 30
```

```go
// Bad
const MAX_RETRY_COUNT = 3
const default_timeout = 30
```

### インターフェース

単一メソッドのインターフェースには `-er` 接尾辞を使用する。

```go
// Good
type Reader interface {
    Read([]byte) (int, error)
}

type Writer interface {
    Write([]byte) (int, error)
}
```

```go
// Bad
type FileReader interface {
    Read([]byte) (int, error)
}

type FileWriter interface {
    Write([]byte) (int, error)
}
```

---

## 4. パッケージ設計

### パッケージ名

パッケージ名は簡潔で意味のあるものにする。

```go
// Good

// Package httputil は HTTP の共通ユーティリティを提供します。
package httputil
```

```go
// Bad

// パッケージ名から用途が分かりにくい
package common
```

### インポート

インポートは以下の順序でグループ化する。

1. 標準ライブラリ
2. プロジェクト・サードパーティパッケージ
3. Protocol Buffer
4. サイドエフェクトのみのインポート

```go
// Good
import (
    "fmt"
    "net/http"

    "github.com/gin-gonic/gin"

    "myproject/internal/config"
)
```

```go
// Bad
import (
    "github.com/gin-gonic/gin"
    "fmt"
    "myproject/internal/config"
    "net/http"
)
```

### インポートのリネーム

基本的にはインポートしたパッケージをリネームしない。

ただし、以下の場合はリネームしてよい。

* 名前が衝突する場合
* パッケージ名が分かりにくい場合

```go
// Good
import (
    foosvcpb "path/to/package/foo_service_go_proto"
)
```

リネームする場合は、アンダースコアや大文字を使用せず、コードベース全体で一貫した名前を使用する。

```go
// Good
import (
    foosvcpb "path/to/package/foo_service_go_proto"
)
```

```go
// Bad
import (
    FooSvcPB "path/to/package/foo_service_go_proto"
)
```

```go
// Bad
import (
    foo_svc_pb "path/to/package/foo_service_go_proto"
)
```

### `import .`

`import .` は使用しない。

```go
// Bad
import (
    "bar/testutil"
    . "foo"
)

var myThing = Bar()
```

```go
// Good
import (
    "bar/testutil"
    "foo"
)

var myThing = foo.Bar()
```

### `import _`

副作用のみを目的とした blank import は、原則として `main` パッケージまたは必要なテストファイルでのみ使用する。

ライブラリパッケージでは避ける。

```go
// Good
package main

import (
    _ "github.com/go-sql-driver/mysql"
)
```

```go
// Bad
package config

import (
    _ "github.com/go-sql-driver/mysql"
)
```

---

## 5. インターフェース

インターフェースは、実装するパッケージではなく、**利用するパッケージ側**で定義する。

```go
// Good

package consumer

type Thinger interface {
    Thing() bool
}

func Foo(t Thinger) string {
    // ...
}
```

```go
// Bad

// 実装側で利用者のためのインターフェースを定義している
package producer

type Thinger interface {
    Thing() bool
}

type Thing struct {
    // ...
}
```

実装側は可能な限り具象型を返す。

```go
// Good
func NewClient() *Client {
    return &Client{}
}
```

```go
// Bad
func NewClient() ClientInterface {
    return &Client{}
}
```

---

## 6. エラーハンドリング

Go ではエラーを明示的に処理する。

### エラーを無視しない

基本的に、関数はエラーを追加の戻り値として返す。

```go
// Good
value, ok := Lookup(key)
if !ok {
    return fmt.Errorf("キー %q の値が見つかりません", key)
}

return Parse(value)
```

```go
// Bad
value, _ := Lookup(key)

return Parse(value)
```

### エラーは最後の戻り値にする

```go
// Good
func ParseUser(data []byte) (User, error) {
    var u User

    err := json.Unmarshal(data, &u)

    return u, err
}
```

```go
// Bad
func ParseUser(data []byte) (error, User) {
    var u User

    err := json.Unmarshal(data, &u)

    return err, u
}
```

### エラーのラップ

エラーを追加情報とともに返す場合は `fmt.Errorf` と `%w` を使用する。

```go
// Good
func ReadConfig(filename string) (*Config, error) {
    data, err := os.ReadFile(filename)
    if err != nil {
        return nil, fmt.Errorf("設定ファイルの読み込みに失敗しました: %w", err)
    }

    var config Config
    if err := json.Unmarshal(data, &config); err != nil {
        return nil, fmt.Errorf("設定ファイルの解析に失敗しました: %w", err)
    }

    return &config, nil
}
```

```go
// Bad
func ReadConfig(filename string) (*Config, error) {
    data, err := os.ReadFile(filename)
    if err != nil {
        return nil, fmt.Errorf("設定ファイルの読み込みに失敗しました: %v", err)
    }

    var config Config
    if err := json.Unmarshal(data, &config); err != nil {
        return nil, fmt.Errorf("設定ファイルの解析に失敗しました: %v", err)
    }

    return &config, nil
}
```

`%v` では元のエラーとのラップ関係が維持されないため、エラーをラップする場合は `%w` を使用する。

### エラー処理を先に行う

エラー処理後は早期 `return` し、正常系を `else` の中に入れない。

```go
// Good
x, err := f()
if err != nil {
    // エラー処理
    return
}

// x を使う処理
```

```go
// Bad
if x, err := f(); err != nil {
    // エラー処理
    return
} else {
    // x を使う処理
}
```

### In-band error を避ける

`-1`、`null`、空文字列などの特殊な値をエラーとして利用しない。

```go
// Bad
func Lookup(key string) int
```

代わりに複数戻り値を使用する。

```go
// Good
func Lookup(key string) (value string, ok bool)
```

---

## 7. `panic` の扱い

通常の処理では `panic()` を使用しない。

エラーを返せる場合は `return err` を使用する。

```go
// Good
func Process() error {
    if err := doSomething(); err != nil {
        return err
    }

    return nil
}
```

```go
// Bad
func Process() error {
    if err := doSomething(); err != nil {
        panic(err)
    }

    return nil
}
```

### `Must` パターン

プログラム起動時など、失敗したらプログラム自体を終了させるべきセットアップ処理では `Must` パターンを使用できる。

```go
// Good
func MustParse(version string) *Version {
    v, err := Parse(version)
    if err != nil {
        log.Fatalf("MustParse(%q) = _, %v", version, err)
    }

    return v
}
```

```go
// Bad
func ValidateUserInput(version string) *Version {
    v, err := Parse(version)
    if err != nil {
        log.Fatalf("入力値の解析に失敗しました: %v", err)
    }

    return v
}
```

`Must` 関数はユーザー入力のバリデーションなどには使用せず、プログラム起動時の前提条件などに限定する。

---

## 8. 関数・メソッド設計

### レシーバー名

レシーバー名は短く、一貫した名前を使用する。

```go
type User struct {
    Name string
    Age  int
}
```

```go
// Good
func (u User) String() string {
    return fmt.Sprintf("%s (%d)", u.Name, u.Age)
}
```

```go
// Bad
func (user User) String() string {
    return fmt.Sprintf("%s (%d)", user.Name, user.Age)
}
```

### 値レシーバー

レシーバーを変更しない場合に使用する。

```go
// Good
func (u User) String() string {
    return fmt.Sprintf("%s (%d)", u.Name, u.Age)
}
```

```go
// Bad
func (u *User) String() string {
    return fmt.Sprintf("%s (%d)", u.Name, u.Age)
}
```

### ポインタレシーバー

レシーバーを変更する場合に使用する。

```go
// Good
func (u *User) UpdateAge(age int) {
    u.Age = age
}
```

```go
// Bad
func (u User) UpdateAge(age int) {
    u.Age = age
}
```

値レシーバーでは `u` のコピーを変更しているため、呼び出し元の `User` は変更されない。

### レシーバーの統一

同じ型のメソッドでは、基本的にポインタレシーバーか値レシーバーのどちらかに統一する。

```go
// Good
type User struct {
    Name string
    Age  int
}

func (u *User) UpdateAge(age int) {
    u.Age = age
}

func (u *User) String() string {
    return fmt.Sprintf("%s (%d)", u.Name, u.Age)
}
```

```go
// Bad
type User struct {
    Name string
    Age  int
}

func (u *User) UpdateAge(age int) {
    u.Age = age
}

func (u User) String() string {
    return fmt.Sprintf("%s (%d)", u.Name, u.Age)
}
```

### コピーしてはいけない型

`sync.Mutex` や `bytes.Buffer` など、コピーによって問題が発生する型はコピーしない。

```go
// Bad
mu := sync.Mutex{}
mu2 := mu
```

```go
// Good
mu := &sync.Mutex{}
```

コピーされて困るフィールドを持つ構造体も、基本的にポインタレシーバーを使用する。

```go
// Good
type Record struct {
    buf bytes.Buffer
}

func New() *Record {
    // ...
}

func (r *Record) Process(...) {
    // ...
}

func Consumer(r *Record) {
    // ...
}
```

```go
// Bad
type Record struct {
    buf bytes.Buffer
}

func Consumer(r Record) {
    // Record がコピーされる可能性がある
}
```

### 関数の引数

関数シグネチャは簡潔に保つ。

引数が多くなった場合は、構造体にまとめることを検討する。

```go
// Good
type CreateUserInput struct {
    Name     string
    Email    string
    Age      int
    Address  string
    IsActive bool
}

func CreateUser(input CreateUserInput) error {
    // ...
    return nil
}
```

```go
// Bad
func CreateUser(
    name string,
    email string,
    age int,
    address string,
    isActive bool,
) error {
    // ...
    return nil
}
```

### 名前付き戻り値

`naked return` を使用するためだけに名前付き戻り値を使用しない。

```go
// Bad
func (n *Node) Parent1() (node *Node)

func (n *Node) Parent2() (node *Node, err error)
```

```go
// Good
func (n *Node) Parent1() *Node

func (n *Node) Parent2() (*Node, error)
```

ただし、`defer` 内で戻り値を変更する必要がある場合は名前付き戻り値を使用してよい。

```go
// Good
func Process() (err error) {
    defer func() {
        if err != nil {
            err = fmt.Errorf("処理に失敗しました: %w", err)
        }
    }()

    err = doSomething()
    return
}
```

```go
// Bad
func Process() error {
    defer func() {
        // 名前付き戻り値がないため、ここで return 値を変更できない
    }()

    return doSomething()
}
```

---

## 9. 構造体リテラル

構造体を生成するときはフィールド名を明示する。

```go
// Good
good := otherpkg.Type{A: 42}
```

```go
// Bad
r := csv.Reader{',', '#', 4, false, false, false, false}
```

複数行の構造体リテラルでは、括弧のインデントを揃える。

```go
// Good
good := []*Type{
    {Key: "multi"},
    {Key: "line"},
}
```

```go
// Bad
good := []*Type{
{Key: "multi"},
{Key: "line"},
}
```

---

## 10. `switch`

Go の `switch` は各 `case` の末尾で自動的に終了するため、通常の `break` は書かない。

```go
// Good
switch x {
case "A", "B":
    buf.WriteString(x)

case "C":
    // 別の箇所で処理される

default:
    return fmt.Errorf("未知の値です: %q", x)
}
```

```go
// Bad
switch x {
case "A", "B":
    buf.WriteString(x)
    break

case "C":
    break

default:
    return fmt.Errorf("未知の値です: %q", x)
}
```

### ループから抜ける場合

`for` ループから `switch` を抜けたい場合は、ラベル付き `break` を使用する。

```go
// Good
loop:
for {
    switch x {
    case "A":
        break loop
    }
}
```

```go
// Bad
for {
    switch x {
    case "A":
        break
    }
}
```

上記の `break` は `switch` だけを終了し、`for` ループ自体は終了しない。

---

## 11. `context.Context`

`context.Context` は API 境界やプロセス間で、以下の情報を伝搬するために使用する。

* キャンセル
* デッドライン
* 認証情報
* トレース情報

### 関数の第1引数にする

関数シグネチャでは原則として第1引数に置く。

```go
// Good
func F(ctx context.Context, otherArg string) error {
    // ...
    return nil
}
```

```go
// Bad
func F(otherArg string, ctx context.Context) error {
    // ...
    return nil
}
```

### HTTP ハンドラ

HTTP ハンドラでは `req.Context()` を使用する。

```go
// Good
func Handler(w http.ResponseWriter, req *http.Request) {
    ctx := req.Context()

    // ctx を使用する
}
```

```go
// Bad
func Handler(w http.ResponseWriter, req *http.Request) {
    ctx := context.Background()

    // HTTP リクエストの context を使用していない
    _ = ctx
}
```

### エントリーポイント

エントリーポイント関数では `context.Background()` を使用する。

```go
// Good
func main() {
    ctx := context.Background()

    // ...
    _ = ctx
}
```

```go
// Bad
func main() {
    ctx := context.TODO()

    // ...
    _ = ctx
}
```

### テスト

Go 1.24 以降のテストでは `t.Context()` を使用できる。

```go
// Good
func TestSomething(t *testing.T) {
    ctx := t.Context()

    // ...
    _ = ctx
}
```

```go
// Bad
func TestSomething(t *testing.T) {
    ctx := context.Background()

    // テスト専用の context を利用せず Background を直接使用している
    _ = ctx
}
```

### 構造体のフィールドに持たせない

`context.Context` を構造体のフィールドとして保持しない。

```go
// Good
type Server struct {
    // ...
}

func (s *Server) Process(ctx context.Context, req *Request) error {
    // ...
    return nil
}
```

```go
// Bad
type Server struct {
    ctx context.Context
}
```

独自の Context 型は作成せず、`context.Context` を使用する。

```go
// Good
func Process(ctx context.Context) error {
    // ...
    return nil
}
```

```go
// Bad
type RequestContext struct {
    // 独自の Context 型を作成している
}

func Process(ctx *RequestContext) error {
    // ...
    return nil
}
```

---

## 12. 並行プログラミング

`goroutine` を使用する場合は、ライフサイクルと終了条件を明確にする。

```go
func processData(ctx context.Context, data <-chan string) <-chan Result {
    results := make(chan Result)

    go func() {
        defer close(results)

        for {
            select {
            case item := <-data:
                if item == "" {
                    return
                }

                results <- process(item)

            case <-ctx.Done():
                return
            }
        }
    }()

    return results
}
```

```go
// Bad
func processData(data <-chan string) <-chan Result {
    results := make(chan Result)

    go func() {
        for item := range data {
            results <- process(item)
        }
    }()

    return results
}
```

上記のように終了条件やキャンセル処理が明確でない `goroutine` は、リークにつながる可能性がある。

`goroutine` のリークやデータ競合を避けるため、`goroutine` がいつ終了するのかを明確にする。

### 同期処理を優先する

可能な場合は、非同期関数より同期関数を優先する。

```go
// Good
func Lookup(key string) (Value, error) {
    // キーに対応する値を直接返す
}
```

```go
// Bad
func LookupAsync(key string) <-chan Value {
    result := make(chan Value)

    go func() {
        result <- lookup(key)
    }()

    return result
}
```

並行処理は、必要になった側で追加できるようにする。

---

## 13. コメント

公開された名前にはコメントを付ける。

コメントは対象となる名前から始め、完全な文章として記述する。

```go
// Package math は基本的な数学関数を提供します。
package math

// Pi は数学定数 π を表します。
const Pi = 3.14159265358979323846

// Sqrt は x の平方根を返します。
// x が負の場合は panic します。
func Sqrt(x float64) float64 {
    if x < 0 {
        panic("math: 負の値の平方根が指定されました")
    }

    // 実装...
    return 0
}
```

```go
// Bad

// 数学関数
package math

// 円周率
const Pi = 3.14159265358979323846

// 平方根を計算
func Sqrt(x float64) float64 {
    // ...
    return 0
}
```

公開された名前のコメントでは、対象となる名前から始める。

### パッケージコメント

パッケージコメントは `package` 宣言の直前に記述する。

```go
// Good

// Package math は基本的な定数と数学関数を提供します.
//
// このパッケージはアーキテクチャ間でビット同一の結果を保証しません.
package math
```

```go
// Bad

package math

// Package math は基本的な定数と数学関数を提供します.
```

パッケージごとにパッケージコメントは1つだけにする。

```go
// Good

// Package math は基本的な定数と数学関数を提供します.
package math
```

```go
// Bad

// Package math は基本的な定数と数学関数を提供します.
package math

// Package math は数学に関する処理を提供します.
```

コメントが長くなる場合は、`doc.go` にパッケージコメントと `package` 宣言だけを配置してもよい。

```go
// doc.go

// Package math は基本的な定数と数学関数を提供します.
//
// このパッケージはアーキテクチャ間でビット同一の結果を保証しません.
package math
```

---

## 14. テスト

Go 標準の `testing` パッケージを使用する。

テストファイルは `_test.go` とする。

```text
user_test.go
```

```text
// Bad
userTest.go
```

### テスト命名

テスト関数は `Test` に続けて対象の型・関数・メソッドなどを表す名前にする。

```go
// Good
func TestUser_UpdateAge(t *testing.T) {
    // ...
}
```

```go
// Bad
func Test1(t *testing.T) {
    // ...
}
```

### テーブル駆動テスト

同じロジックで複数のケースをテストする場合は、テーブル駆動テストを使用する。

```go
// Good
func TestCompare(t *testing.T) {
    compareTests := []struct {
        a, b string
        want int
    }{
        {"", "", 0},
        {"a", "", 1},
        {"", "a", -1},
        {"abc", "abc", 0},
    }

    for _, test := range compareTests {
        got := Compare(test.a, test.b)

        if got != test.want {
            t.Errorf(
                "Compare(%q, %q) = %v, want %v",
                test.a,
                test.b,
                got,
                test.want,
            )
        }
    }
}
```

```go
// Bad
func TestCompare(t *testing.T) {
    if Compare("", "") != 0 {
        t.Error("空文字列同士の比較に失敗しました")
    }

    if Compare("a", "") != 1 {
        t.Error("a と空文字列の比較に失敗しました")
    }

    if Compare("", "a") != -1 {
        t.Error("空文字列と a の比較に失敗しました")
    }

    if Compare("abc", "abc") != 0 {
        t.Error("abc 同士の比較に失敗しました")
    }
}
```

### テストケース名

テストケースには意味のある名前を付ける。

```go
// Good
for _, d := range data {
    t.Run(d.name, func(t *testing.T) {
        got := Translate(d.srcLang, d.dstLang, d.srcText)

        if got != d.wantDstText {
            t.Errorf(
                "%s\nTranslate(%q, %q, %q) = %q, want %q",
                d.desc,
                d.srcLang,
                d.dstLang,
                d.srcText,
                got,
                d.wantDstText,
            )
        }
    })
}
```

```go
// Bad
for i, d := range data {
    t.Run(fmt.Sprintf("%d", i), func(t *testing.T) {
        got := Translate(d.srcLang, d.dstLang, d.srcText)

        if got != d.wantDstText {
            t.Errorf("テストに失敗しました")
        }
    })
}
```

テストケースのインデックスをテスト名として使用しない。

### サブテスト

サブテストは、他のテストケースの実行結果や初期状態に依存させない。

```go
// Good
for _, test := range tests {
    test := test

    t.Run(test.name, func(t *testing.T) {
        // 各テストケースで独立した状態を使用する
        got := process(test.input)

        if got != test.want {
            t.Errorf("結果が期待値と一致しません: got=%v, want=%v", got, test.want)
        }
    })
}
```

```go
// Bad
var sharedState State

for _, test := range tests {
    t.Run(test.name, func(t *testing.T) {
        // 他のテストケースの結果によって sharedState の状態が変わる
        sharedState = processState(sharedState, test.input)
    })
}
```

`go test -run` で個別に実行できるよう、サブテスト名は読みやすくする。

```bash
go test -run 'TestUser/正常なメールアドレス'
```

```go
// Bad
go test -run 'TestUser/0'
```

### テストパッケージ

#### テスト対象と同じパッケージ

```go
package foo
```

以下のような場合に使用する。

* `unexported` な識別子にアクセスしたい場合
* 実装詳細を直接テストしたい場合

```go
// Good
package foo

func TestParseInternalValue(t *testing.T) {
    // foo パッケージ内の非公開関数をテストできる
    got := parseInternalValue("test")

    // ...
    _ = got
}
```

```go
// Bad
package foo_test

func TestParseInternalValue(t *testing.T) {
    // 非公開関数にはアクセスできない
    // parseInternalValue("test")
}
```

#### 外部パッケージとしてテスト

```go
package fireworks_test
```

以下のような場合に使用する。

* 循環依存を避けたい場合
* 外部利用者と同じ視点でテストしたい場合
* 統合テストなどを行う場合

```go
// Good
package fireworks_test

func TestFireworks(t *testing.T) {
    // 外部利用者と同じ公開 API を通してテストする
}
```

```go
// Bad
package fireworks_test

func TestFireworks(t *testing.T) {
    // 非公開実装の詳細に直接依存する
    // fireworks.internalState = ...
}
```

### テストヘルパー

`*testing.T` を受け取るテストヘルパーでは `t.Helper()` を呼び出す。

```go
// Good
func readFile(t *testing.T, filename string) string {
    t.Helper()

    contents, err := runfiles.ReadFile(filename)
    if err != nil {
        t.Fatal(err)
    }

    return string(contents)
}
```

```go
// Bad
func readFile(t *testing.T, filename string) string {
    contents, err := runfiles.ReadFile(filename)
    if err != nil {
        t.Fatal(err)
    }

    return string(contents)
}
```

`context.Context` と `*testing.T` の両方を受け取る場合は、`context.Context` を先に置く。

```go
// Good
func readTestFile(ctx context.Context, t *testing.T, path string) string {
    t.Helper()

    // ...
    return ""
}
```

```go
// Bad
func readTestFile(t *testing.T, ctx context.Context, path string) string {
    t.Helper()

    // ...
    return ""
}
```

### `MustXYZ`

セットアップに失敗した場合にテストを停止させるヘルパーは `MustXYZ` / `mustXYZ` と命名する。

```go
// Good
func mustMarshalAny(t *testing.T, m proto.Message) *anypb.Any {
    t.Helper()

    any, err := anypb.New(m)
    if err != nil {
        t.Fatalf("mustMarshalAny(t, m) = %v; エラーが発生しないことを期待しました", err)
    }

    return any
}
```

```go
// Bad
func marshalAnyOrFail(t *testing.T, m proto.Message) *anypb.Any {
    t.Helper()

    any, err := anypb.New(m)
    if err != nil {
        t.Fatalf("エラーが発生しました: %v", err)
    }

    return any
}
```

---

## 15. パフォーマンス

まず正しいコードを書くことを優先する。

パフォーマンス最適化は、ボトルネックを分析した上で行う。

### slice の容量を事前確保

必要なサイズが事前に分かる場合は、容量を指定して確保する。

```go
// Good
func processItems(items []string) []Result {
    results := make([]Result, 0, len(items))

    for _, item := range items {
        results = append(results, process(item))
    }

    return results
}
```

```go
// Bad
func processItems(items []string) []Result {
    var results []Result

    for _, item := range items {
        results = append(results, process(item))
    }

    return results
}
```

### `strings.Builder`

文字列を大量に連結する場合は `strings.Builder` を使用する。

```go
// Good
func buildMessage(parts []string) string {
    var builder strings.Builder

    builder.Grow(estimateSize(parts))

    for _, part := range parts {
        builder.WriteString(part)
    }

    return builder.String()
}
```

```go
// Bad
func buildMessage(parts []string) string {
    var message string

    for _, part := range parts {
        message += part
    }

    return message
}
```

パフォーマンス上の問題を調査する場合は、`pprof` などのツールを使用する。

```go
// Good
// pprof などを使用してボトルネックを特定してから最適化する。
```

```go
// Bad
// ボトルネックを調査せず、推測だけでコードを複雑化する。
```

---

## 16. `any`

Go 1.18 以降では `interface{}` の代わりに `any` を使用する。

```go
// Good
func Process(data any) error {
    // ...
    return nil
}
```

```go
// Bad
func Process(data interface{}) error {
    // ...
    return nil
}
```

---

## 17. `crypto/rand`

鍵やトークンなど、セキュリティに関わる乱数生成では `math/rand` を使用しない。

`crypto/rand` を使用する。

```go
// Good
import (
    "crypto/rand"
    "fmt"
)
```

```go
// Bad
import (
    "fmt"
    "math/rand"
)
```

セキュリティ用途では、予測可能な乱数生成器を使用しない。

```go
// Good
func GenerateToken() ([]byte, error) {
    token := make([]byte, 32)

    if _, err := rand.Read(token); err != nil {
        return nil, fmt.Errorf("トークンの生成に失敗しました: %w", err)
    }

    return token, nil
}
```

```go
// Bad
func GenerateToken() []byte {
    token := make([]byte, 32)

    for i := range token {
        token[i] = byte(rand.Intn(256))
    }

    return token
}
```

---

## 18. `fmt.Printf` の `%q`

人間向けの出力では、手動でクォートを付けるのではなく `%q` を使用する。

```go
// Good
fmt.Printf("値 %q は英語のテキストのように見えます", someText)
```

```go
// Bad
fmt.Printf("値 \"%s\" は英語のテキストのように見えます", someText)

fmt.Printf("値 '%s' は英語のテキストのように見えます", someText)
```

---

## 19. コマンドラインフラグ

フラグは `package main` 内でのみ定義する。

```go
// Good
package main

var (
    pollInterval = flag.Duration(
        "poll_interval",
        time.Minute,
        "ポーリングに使用する間隔",
    )
)
```

```go
// Bad
package config

var (
    pollInterval = flag.Duration(
        "poll_interval",
        time.Minute,
        "ポーリングに使用する間隔",
    )
)
```

汎用パッケージでは、コマンドラインフラグではなく Go API を通じて設定できるようにする。

```go
// Good
type Config struct {
    PollInterval time.Duration
}

func NewClient(config Config) *Client {
    // ...
    return &Client{}
}
```

```go
// Bad
func NewClient() *Client {
    // ライブラリ側でコマンドラインフラグに直接依存する
    // ...
    return &Client{}
}
```

### フラグ名

フラグ名には `snake_case` を使用し、Go の変数名には `MixedCaps` を使用する。

```go
// Good
var (
    pollInterval = flag.Duration(
        "poll_interval",
        time.Minute,
        "ポーリングに使用する間隔",
    )
)
```

```go
// Bad
var (
    poll_interval = flag.Int(
        "pollIntervalSeconds",
        60,
        "ポーリングに使用する間隔（秒）",
    )
)
```

### フラグのグローバル変数

フラグのグローバル変数は、imports 直後に独立した `var` グループとして配置する。

```go
// Good
package main

import (
    "flag"
    "time"

    "myproject/internal/config"
)

var (
    pollInterval = flag.Duration(
        "poll_interval",
        time.Minute,
        "ポーリングに使用する間隔",
    )
)
```

```go
// Bad
package main

import (
    "flag"
    "time"

    "myproject/internal/config"
)

func main() {
    var (
        pollInterval = flag.Duration(
            "poll_interval",
            time.Minute,
            "ポーリングに使用する間隔",
        )
    )

    // ...
    _ = pollInterval
}
```

---

## 20. ロギング

不要なフォーマット指定は避ける。

```go
// Good
log.Info(value)
```

```go
// Bad
log.Infof("%v", value)
```

フォーマットが必要な場合は、フォーマット用のメソッドを使用する。

```go
// Good
log.Infof("ユーザー ID: %d", userID)
```

```go
// Bad
log.Info(fmt.Sprintf("ユーザー ID: %d", userID))
```

---

## 21. 変更しない方針

以下のような項目については、プロジェクト内で一貫性を保つことを優先する。

* ゼロ値によるローカル変数初期化
* `new` と空の複合リテラルの使い分け
* `make` と空の複合リテラルの使い分け
* `cmp.Diff` の `got` / `want` の順序
* `errors.New` と `fmt.Errorf` の使い分け

これらについて不要に独自ルールを追加しない。

### ゼロ値によるローカル変数初期化

プロジェクト内で一貫している場合は、その書き方を維持する。

```go
// Good
var users []User
```

```go
// Good
users := []User{}
```

どちらを採用するかについて、このコーディング規約で新たな独自ルールを追加しない。

```go
// Bad
// プロジェクト内で既存の書き方が統一されているにもかかわらず、
// 個人の好みだけで別の書き方へ変更する。
```

### `new` と空の複合リテラルの使い分け

プロジェクト内で一貫している場合は、その書き方を維持する。

```go
// Good
user := new(User)
```

```go
// Good
user := &User{}
```

どちらを採用するかについて、このコーディング規約で新たな独自ルールを追加しない。

```go
// Bad
// プロジェクト内で既存の書き方が統一されているにもかかわらず、
// 個人の好みだけで new と複合リテラルを変更する。
```

### `make` と空の複合リテラルの使い分け

プロジェクト内で一貫している場合は、その書き方を維持する。

```go
// Good
users := make([]User, 0)
```

```go
// Good
users := []User{}
```

どちらを採用するかについて、このコーディング規約で新たな独自ルールを追加しない。

```go
// Bad
// プロジェクト内で既存の書き方が統一されているにもかかわらず、
// 個人の好みだけで make と複合リテラルを変更する。
```

### `cmp.Diff` の `got` / `want` の順序

プロジェクト内で一貫している場合は、その書き方を維持する。

```go
// Good
if diff := cmp.Diff(want, got); diff != "" {
    t.Errorf("差分 (-want +got):\n%s", diff)
}
```

```go
// Good
if diff := cmp.Diff(got, want); diff != "" {
    t.Errorf("差分 (-got +want):\n%s", diff)
}
```

プロジェクト内ですでに順序が統一されている場合は、不要に変更しない。

```go
// Bad
// 「個人的にこちらの順序のほうが好み」という理由だけで、
// 既存コード全体の順序を変更する。
```

### `errors.New` と `fmt.Errorf` の使い分け

プロジェクト内で一貫している場合は、その書き方を維持する。

```go
// Good
err := errors.New("ユーザーが見つかりません")
```

```go
// Good
err := fmt.Errorf("ユーザー %q が見つかりません", userID)
```

使い分けについて、このコーディング規約で不要な独自ルールを追加しない。

```go
// Bad
// 既存コードで一貫している書き方を、個人の好みだけで変更する。
```
